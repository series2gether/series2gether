<div class="container p4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    {{#if success}}
                    Acuenta verificada! puedes acceder a tu cuenta desde la barra de navegación o desde <a href="/user/login">aquí</a>.
                    {{else}}
                    Lo siento, no pudimos verificar tu cuenta
                    {{/if}}
                </div>
            </div>
        </div>
    </div>
</div>